name: Run

on:
    push:
        branches:
            - main
    workflow_dispatch: ~
    pull_request:
        branches:
            - main
    schedule:
      - cron: '0 0 * * *'


jobs:
    testing:
        runs-on: ubuntu-latest
        steps:
            - name: Into the python dir
              run: cd python
            - name: Run
              run: ./bin/run
            - name: Archive artifacts
              uses: actions/upload-artifact@v3
              with:
                name: get-best-theme-team
                path: ./files
